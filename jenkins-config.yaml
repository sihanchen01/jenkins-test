jenkins:
  systemMessage: "Jenkins managed by JCasC for local testing!"
  numExecutors: 2
  mode: NORMAL
  scmCheckoutRetryCount: 5
  
  securityRealm:
    local:
      allowsSignup: false
      enableCaptcha: false
      users:
        - id: "admin"
          password: "admin123"
        - id: "developer"
          password: "dev123"
  
  authorizationStrategy:
    globalMatrix:
      permissions:
        - "USER:Overall/Administer:admin"
        - "USER:Overall/Read:authenticated"
        - "USER:Job/Build:authenticated"
        - "USER:Job/Cancel:authenticated"
        - "USER:Job/Read:authenticated"
        - "USER:Job/Workspace:authenticated"
        - "USER:View/Read:authenticated"

  remotingSecurity:
    enabled: true

unclassified:
  location:
    url: "http://localhost:8080/"
    adminAddress: "admin@localhost"
  
  globalLibraries:
    libraries:
      - name: "shared-library"
        defaultVersion: "main"
        retriever:
          modernSCM:
            scm:
              git:
                remote: "https://github.com/your-org/jenkins-shared-library.git"

credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              scope: GLOBAL
              id: "github-creds"
              username: "your-github-user"
              password: "your-github-token"
              description: "GitHub credentials for repository access"
          - string:
              scope: GLOBAL
              id: "api-token"
              secret: "your-secret-api-token"
              description: "API token for external services"

jobs:
  - script: |
      pipelineJob('example-pipeline') {
        definition {
          cps {
            script('''
              pipeline {
                agent any
                stages {
                  stage('Hello') {
                    steps {
                      echo 'Hello World from JCasC!'
                    }
                  }
                }
              }
            ''')
            sandbox()
          }
        }
      }
